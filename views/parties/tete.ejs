<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/public/css/bootstrap.min.css" />
        <link rel="stylesheet" href="/public/css/generale.css" />
        <% if (css != "") { %>
            <link rel="stylesheet" href="/public/css/<%= css  %>.css" />
        <% } %>
        <!-- Permet la mise en page de la tete et du pied -->
        <link id="favicon" rel="icon" type="image/x-icon" href="/public/img/favicon-dark.ico" />
        <title><%= titre %> - Dossards RVA</title>
    </head>
    <body>
        <header>
            <nav class="navbar navbar-expand-lg">
                <a class="navbar-brand text-white ms-2" href="https://eloi2.alwaysdata.net/">Dossards</a>
                <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#menu">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <% if(connexion == process.env.CHAINE_COOKIE) {%>
                <div class="collapse navbar-collapse" id="menu">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a href="https://eloi2.alwaysdata.net/liste" class="nav-link text-white ms-3">Liste</a>
                        </li>
                        <li class="nav-item">
                            <a href="https://eloi2.alwaysdata.net/ajout" class="nav-link text-white ms-3">Ajout</a>
                        </li>
                    </ul>
                </div>
                <% } %>
            </nav>
        </header>
